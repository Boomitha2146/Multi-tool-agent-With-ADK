# Multi-tool-agent-With-ADK
Multi-Tool AI Agent built using Agent Development Kit (ADK) that intelligently selects and uses multiple tools to perform tasks, reason over user queries, and generate accurate, context-aware responses using an LLM.
